# Enable only after configuring a bot and explicit channel IDs.
DISCORD_ENABLED=false
DISCORD_TOKEN=
# Host publication; application listens on 3000 inside the container.
HEALTH_BIND_IP=127.0.0.1
HEALTH_PUBLISHED_PORT=3000
IMAGE_TAG=local
APP_REVISION=development
